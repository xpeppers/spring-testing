# Adding this properties let the ITs connect to the real database
# Warning: its the same one used by the application
# would be better to create a test schema
spring.datasource.url = jdbc:postgresql://127.0.0.1:15432/postgres
spring.datasource.username = testuser
spring.datasource.password = password
spring.sql.init.platform = POSTGRESQL

spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = create-drop
spring.jpa.database-platform = org.hibernate.dialect.PostgreSQLDialect